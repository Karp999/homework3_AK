# **Инструкция по работе с GIT в VS Code. ЧАСТЬ 2. Ветвление (ЧАСТЬ 1 ниже)** #
В первой части были рассмотрены базовые команды для работы с GIT в VS Code, а также основа синтаксиса языка Markdown.
В данной части инстукции будет рассмотрено ветвление.

1. ## ***Определение ветвления*** ##
*"Ветвление"* можно понимать, как процесс создания параллельной копии основного файла или части файла, который называется *"Ветка"*.

*"Ветка"* чаще всего используется в качестве чернового варианта работы с основным документом.
Также "ветка – это последовательность коммитов". 

**! Преимущество веток в их независимости.**
Можно менять файлы в одной ветке, и изменения не отобразятся в другой, пока создатель не захочет соединить эти ветки вместе. Изначально в репозитории одна ветка.


2. ## ***Создание ветки и переход на другую ветку.*** ##
Чтобы создать ветку, необходимо выполнить команду:

**git branch** (здесь написать имя ветки,которую создаёте без скобок)

После создания ветки можно посмотреть, на какой ветке вы находитесь (и одновременно проверить, какие ветки имеются в репозитории - для самоконтроля), для этого можно воспользоваться командой:

**git branch**

    !Уточнение: в терминале знак * стоит рядом с веткой, в которой вы сейчас находитесь. Также указателем ветки, в которой производилась/-ся работа является слово "HEAD".
Для того, чтобы перейти на другую ветку используется команда:

**git checkout** (название нужной ветки без кавычек)

3. ## ***Слияние веток*** ##
Чтобы соединить/слить ветку, в которой были произведеные необходимые действия, с основной веткой, необходимо выполнить команду:

**git merge** (здесь имя ветки, которую нужно слить с основной)

*!Уточнение:* "git merge" вызывается из той ветки, куда нужно "влить" изменённую/черновую.

*! Предостережение:* при слиянии веток может возникнуть конфликт, если в основной и вливаемой ветке в одном и том же месте текста есть разница. Обычно конфликты возникают, когда два человека изменяют одни и те же строки в файле или один разработчик удаляет файл, который в это время изменяет другой разработчик. В таких случаях Git не может автоматически определить, какое изменение является правильным. Конфликты затрагивают только того разработчика, который выполняет слияние. 
Программа сама предложит три варианта: слить с сохранением основного текста, слить с сохранением изменённого текста, слить с выбором изменения.


4. ## ***Удаление веток*** ##
Если ветки выполнили все функции, которые должны были осуществить, то мы их удаляем.
Для удаления ветки нужно выполнить команду: 

**git branch -d** (здесь имя ветки, которую удаляем, без скобок)



5. ## ***Заключение*** ##
Во второй части данной инструкции рассмотрены основные команды ветвления:

**git branch** (здесь написать имя ветки,которую создаёте без скобок)

**git branch**

**git checkout** (название нужной ветки без кавычек)

**git merge** (здесь имя ветки, которую нужно слить с основной)

**git branch -d** (здесь имя ветки, которую удаляем, без скобок)

Были созданы ветки:
    
    create_branch
    definition
    delete_branch
    * fordeleting
    master
    merge

Попрактиковалась в создании и решении  конфликтов с каждой из созданных веток. Я оставляла оба варианта конфликтов и корректировала тексты в нужную мне сторону.

Попрактиковалась в удалении веток: слила fordeleting c master и удалила её. Создала ветку conflict для конфликта, который оставила для демонстрации преподавателю (не уверена, что мои решённые конфликты будут отображены):

    * conflict
    create_branch
    definition
    delete_branch
    master
    merge
Конфликт в заключении (п.5).
Также пыталась слить master с conflict и сохранить с ошибкой-конфликтом, но не вышло. Сохранилось с корявым текстом. Попробовала откатить слив, не вышло. Решила вернуться к master, скорректировать текст, закоммититься, скорректировать текст в conflict, закоммититься, а потом создать новый конфликт.

В ЧАСТЬ 1 инструкции п.5 (ниже) добавила команды, рассмотренные на втором семинаре:

**git diff**

**clean**

**git log --graph**

Ниже ЧАСТЬ 1 данной инструкции.

# **Инструкция по работе с GIT в VS Code. ЧАСТЬ 1. Введение.** #

Предварительно в Git можно выполнить команды,которые позволят отметить автора выполненных действий:

**git config --global user.name "Name in English.Example: Anya"**

**git config --global user.email example-E-adress@mail.com**

1. ## ***Инициализация*** ##
Когда вы вошли в Visual Studio Code, открыли репозиторий и создали в нём файл (или же открыли имеющийся файл(-ы)), ваша работа с ним будет производиться в поле "Terminal". 
Для начала необходимо инициализироваться. 
Для этого воспользуйтесь командой:

**git init**

2. ## ***Статус*** ##
Для того, чтобы узнать о текущем состоянии репозитория (есть ли изменённые, добавленные, удалённые, неотслеживаемые файлы), воспользуйтесь командой:

**git status**

Статус желательно проверять после каждого сохранения файла, чтобы убедиться, что файл редактируется/сохраняется в нужной вам последовательности, и ничто не теряется из вида.

3. ## ***Добавление файлов*** ##
Для того, чтобы добавить файл, команда:

**git add** (здесь вставить имя файла с расширением. Пример: instruction.md)

4. ## ***Сохранение коммитов*** ##
*"Коммитом"* называется коммит пакет изменений в файле(изменения,добавления,удаления). Изменения в файлах можно зафиксировать командой:

**git commit -m "Комментарий изменения"**

Изменения фиксируются после каждого изменения в файле.

Также можно нажать сочетание клавиш для сохранения текущего состояния:

**Ctrl+S**

5. ## ***История сохранений*** ##
Чтобы проверить историю всех коммитов/сохранений , воспользуйтесь командой:

**git log**

Чтобы посмотреть разницумежду текущим состоянием файла и сохраненной версией, необходимо выполнить команду:

**git diff**

Для очистки поля в терминале (если в нём много команд и неудобно работать), можно использовать команду:

**clean**

Чтобы просмотреть древовидную структуру графа истории веток в ASCII-виде, нужно использовать команду:

**git log --graph**

6. ## ***Синтаксис языка Markdown*** ## 

Для форматирования файла в VS Code с Git  можно использовать язык разметки Markdown. Ниже в подпунктах описаны основные правила/синтаксис использования языка.

* ### *Форматирование текста* ###

Для создания полужирного текста используются двойные звёздочки (**) с двух сторон текста без пробела.

Пример: **Hello**

Для создания курсивного текста используются одинарные звёздочки (*) с двух сторон текста без пробела.

Пример: *Hello*

Для создания полужирного курсивного текста используются тройные звёздочки (***) с двух сторон текста без пробела.

Пример: ***Hello***

Для создания зачеркнутого текста используются двойные тильды (~) с двух сторон текста без пробела.

Пример: ~~Hello~~


* ### *Выделение заголовков* ###

Выделение заголовков производится при помощи: от одного до шести знаков решётки (#) с двух сторон текста с пробелами. Пример (использовать без скобок):

(# Заголовок первого уровня #)

(## Заголовок первого уровня ##)

(### Заголовок третьего уровня ###)

(#### Заголовок четвертого уровня ####)

(##### Заголовок пятого уровня #####)

(###### Заголовок шестого уровня ######)


Также можно использовать знаки: = (для заголовка первого уровня) или - (для заголовка второго уровня) под текстом, который нужно выделить в заголовок. Нужный знак необходимо вводить на всю длину текста без пробелов. Пример (использовать без скобок):

Заголовок первого уровня
(=====================)

Заголовок второго уровня        
(------------------------)

* ### *Списки* ###

Посредством Markdown можно создать списки: нумерованные, маркированные(ненумерованные) и вложенные.Пример:

**Нумерованные списки.** Каждый элемент списка прописывается с новой строки с использованием в начале текста цифры,точки и пробела (1.Текст1, (с новой строки) 2.Текст2, (с новой строки) 3.Текст3):
1.	Текст1
2.	Текст2
3.	Текст3

**Маркированные(ненумерованные) списки.** Каждый элемент списка прописывается с новой строки с использованием в начале текста знака +, - или *):
* Текст1
- Текст2
+ Текст3

**Вложенные списки.** Каждый элемент списка прописывается с новой строки с использованием отступов. Пример:

    привет

    привет

    привет

* ### *Изображение* ###

В Markdown изображение можно вставить c помощью указания URL-адреса изображения. Синтаксис данной команды выглядит следующим образом:

    восклацательный знак + необходимый текст/сообщение(описывающее изображение в квадратных скобках + ссылка/путь к изображению в круглых скобках. 
    Пример: 
    ![Сообщение-текст](/путь/к/изображению.jpg)

    или

    восклацательный знак + необходимый текст/сообщение(описывающее изображение) в квадратных скобках + ссылка/путь к изображению в круглых скобках + в кавычках имя файла (что будет отображаться вместо ссылки).
    Пример: 
    ![Сообщение-текст](/путь/к/изображению.jpg "Подсказка")
Пример-отображение:

![Цветок](rose.jpg)

* ### *Ссылки* ###
Markdown поддерживает два стиля оформления ссылок:

    - гиперссылка, с немедленным указанием адреса (внутритекстовая). 
    Пример:
    [Текст ссылки](http://example.com/ "Заголовок ссылки")

    - гиперссылка, подобная сноске - альтернативный способ задавать ссылки если есть повторения:
    [текст ссылки][example].
    
    При создании сносной гиперссылки вместо целевого адреса используется вторая пара квадратных скобок, внутри которых помещается метка, идентификатор ссылки (id).

    [пример][id]:
    
    Также, можно использовать пробел, чтобы отделять 2 пары квадратных скобок:
    
    [пример] [id]: 
    
    В этом случае возможно определить идентификатор в любом месте документа:
    
    [id]: http://example.com/ "Необязательная подсказка"
    
    В результате на экран выводится следующее: [пример] [id] [id]: http://example.com/ "Необязательная подсказка" 

    Идентификаторы ссылок могут состоять из букв, цифр, пробелов и знаков пунктуации, однако они не чувствительны к регистру. То есть эти два варианта эквивалентны:
    
    [текст ссылки][a]
    [текст ссылки][A]
    
    Markdown позволяет также использовать неявно выраженный идентификатор (сокращенный). В этом случае метка не приводится, вместо неё текст гиперссылки используется и в качестве её имени, а вторая пара квадратных скобок остаётся пустою. Например, чтобы сделать слово «Example» гиперссылкой, ведущей на сайт http://example.com/, достаточно написать:
    
    [Example][]
    
    и затем определить гиперссылку:
    
    [Example]: http://example.com/
    
    В результате на экран выводится следующее: [Example][] [Example]: http://example.com
Пример: 
[Прочитай стихотворение Бродского](brodsky.txt)

* ### *Таблицы* ###
Для создания таблицы в Markdown мы используем тире - и вертикальные полосы | для разделения строк и столбцов.

В первой строке таблицы мы строим заголовок, разделяя эту строку тремя или более дефисами ---, чтобы процессор Markdown понимал форматирование.

Разделение столбцов выполняется с помощью вертикальной черты |, которую программисты также называют pipe.

Пример(в разметке):

    | Заголовок  | Заголовок   |
    | ------- | -------- |
    | Текст   | Текст    |
    | Текст   | Текст    |
Пример(в отображении):
| Заголовок  | Заголовок   |
| ------- | -------- |
| Текст   | Текст    |
| Текст   | Текст    |

* ### *Цитаты* ###
Для цитирования используется щнак >.
Пример(в разметке):

    > Первый уровень цитирования
    >> Второй уровень цитирования
    >>> Третий уровень цитирования
    > Данный текст будет заключен в HTML-теги <blockquote></blockquote>
Пример(в отображении):
> Первый уровень цитирования
>> Второй уровень цитирования
>>> Третий уровень цитирования

> Данный текст будет заключен в HTML-теги <blockquote></blockquote>

7. ## ***Заключение*** ##
В данной инструкции разобраны основные команды работы с Git в VS Code, а также синтаксис языка разметки Markdown, который чаще всего используется для работы с Git в VS Code.

***Основные команды Git в VS Code:***
    
    git config --global user.name "Name in English.Example: Anya"
    git config --global user.email example-E-adress@mail.com
    git init
    git status
    git add (здесь вставить имя файла с расширением. Пример: instruction.md)
    git commit -m "Комментарий изменения"
    git log
Синтаксис языка Markdown подробно прописан в пункте №6.

